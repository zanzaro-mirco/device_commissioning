# ble_bridge_example

L'esempio minimo del plugin: permessi, scansione, connessione e lettura dello stato di una
centralina. Serve anche alla pipeline per compilare il Kotlin e il C del plugin, e per
eseguire i test del JNI su un emulatore.
