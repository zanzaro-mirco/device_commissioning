name: commissioning_protocol
description: Il protocollo di messa in servizio lato app - messaggi, scrittura condizionata alla revisione, esito incerto. Dart puro; il filo (trama e CRC) lo gestisce il plugin nativo.
version: 0.1.0
publish_to: none
repository: https://github.com/zanzaro-mirco/device_commissioning

environment:
  sdk: ^3.4.0

dev_dependencies:
  fake_async: ^1.3.0
  lints: ^6.0.0
  test: ^1.32.0
